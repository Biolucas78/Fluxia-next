import fetch from 'node-fetch';

async function test() {
  // We need a token, but we can just look at the error message.
  // Actually, without a token we get 401. So we can't easily test without token.
}
test();
